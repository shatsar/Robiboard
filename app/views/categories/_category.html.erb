<div class="category">
	<p>
		<%= image_tag(category.bookmarks.last.snapshot.url(:thumb), :class => "img-rounded") unless category.bookmarks.empty? %>
	</p>
	<p><%= category.title %></p>
	<p><%= link_to "view", category %></p>
</div>
