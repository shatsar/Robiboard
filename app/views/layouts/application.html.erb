<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Robiboard" %></title>
    <%= csrf_meta_tags %>
<link href='http://fonts.googleapis.com/css?family=Raleway:400,100' rel='stylesheet' type='text/css'>


    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag '/favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>
	<div id="wrap">
    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="/">Robiboard</a>
            <ul class="nav">
              <li><%= link_to "+Bookmark", new_bookmark_path  %></li>
              <li><%= link_to "Categories", categories_path  %></li>
            </ul>
            
            <%= form_tag '/search', :method => 'GET',:class => "form-search navbar-search pull-right" do %>
            	<div class="input-append">
				<%= text_field_tag :query, @query, :class => "span2 input-medium search-query" %>
            	<button type="submit" class="btn">Search</button>
            	</div>
            <% end %>
        </div>
      </div>
    </div>

    <div class="container-fluid">
          <%= bootstrap_flash %>
          <%= yield %>
      <div id="push"></div>
    </div> <!-- /container -->
    </div>
  <footer>
    <p>&copy; Zonca.org 2013</p>
  </footer>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
